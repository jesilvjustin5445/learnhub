version: '3.8'
services:
  web:
    build: .
    container_name: learnhub-app
    restart: always
    ports:
      - "5000:5000"
    env_file:
      - app/.env
    volumes:
      - ./app/static:/app/static
